cache: apt
dist: trusty
sudo: required

addons:
apt:
  packages:
  - cmake
  - libsdl2-dev
  - libsdl2-image-dev
  - libsdl2-ttf-dev
  - libsdl2-mixer-dev

language: cpp
compiler: gcc
script:
  - cmake CMakeLists.txt
  - make -j4
