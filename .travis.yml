cache: apt
dist: trusty
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y g++ make cmake libsdl2-dev libsdl2-image-dev libsdl2-ttf-dev libsdl2-mixer-dev
language: cpp
compiler: gcc
script:
  - cmake CMakeLists.txt
  - make -j4
